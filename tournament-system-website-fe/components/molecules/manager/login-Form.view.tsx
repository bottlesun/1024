import React from "react";
import InputGroupView, { InputGroupProps } from "../../atoms/forms/input-group.view";

const LoginFormView = (props: InputGroupProps) => {
  return <InputGroupView {...props} />;
};

export default LoginFormView;
